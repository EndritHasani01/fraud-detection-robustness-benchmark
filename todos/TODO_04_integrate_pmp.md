# TODO 04: Integrate PMP (Specialized Method 1)

Goal: run PMP from `Repos/PMP-master/` inside the benchmark so it can be evaluated on the same cached graph variants.

Work:
- Decide the integration strategy:
  - Option A (recommended): patch PMP to load an external DGL graph file produced by the benchmark (cleanest for fairness).
  - Option B: run PMP as a subprocess and parse its stdout logs (faster to wire, harder to keep clean).
- Implement "load external graph" support for PMP:
  - Patch `Repos/PMP-master/DataHelper/datasetHelper.py` so it can load `dgl.load_graphs(graph_path)` when `graph_path` is provided.
  - Ensure it uses the masks from the loaded graph, not newly generated ones.
  - Ensure feature normalization behavior is consistent (document it).
- Make PMP output machine-readable results:
  - Write a short JSON/CSV line with metrics per run (model, dataset, scenario, severity, seed).
  - Avoid relying only on human-readable logs.
- Verify fairness:
  - Same split masks and same perturbed graphs as the baselines.
  - Same metrics and same thresholding rule as the benchmark protocol.

Done when:
- PMP can be run on clean + perturbed graphs generated by the benchmark.
- PMP results appear as rows in `results.csv` alongside MLP and the baseline GNN.

Notes:
- Keep code changes inside `Repos/PMP-master/` minimal and documented. The final report should state any patches you made to run it fairly.

